{
  "name": "@acme/server",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "main": "index.ts",
  "types": "index.ts",
  "config": {
    "prefix": "./"
  },
  "scripts": {
    "clean": "rimraf .turbo node_modules dist",
    "lint": "TIMING=1 eslint \"src/**/*.ts\"",
    "type-check": "tsx --noEmit"
  },
  "dependencies": {
    "@acme/auth": "*",
    "@acme/database": "*",
    "@trpc/client": "^10.8.1",
    "@trpc/server": "^10.8.1",
    "superjson": "1.9.1",
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@acme/config": "*",
    "rimraf": "^3.0.2",
    "eslint": "^8.30.0",
    "typescript": "^4.9.4"
  }
}
