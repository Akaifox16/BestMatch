{
  "name": "@acme/server",
  "version": "0.2.0",
  "license": "UNLICENSED",
  "main": "./index.ts",
  "types": "./index.ts",
  "config": {
    "prefix": "./"
  },
  "scripts": {
    "clean": "rimraf .turbo node_modules dist",
    "lint": "TIMING=1 eslint \"src/**/*.ts\"",
    "type-check": "tsx --noEmit"
  },
  "dependencies": {
    "@acme/auth": "*",
    "@acme/database": "*",
    "@trpc/client": "^10.8.1",
    "@trpc/server": "^10.8.1",
    "bcrypt": "^5.1.0",
    "esbuild": "^0.17.11",
    "superjson": "1.9.1",
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@acme/config": "*",
    "@types/bcrypt": "^5.0.0",
    "eslint": "^8.30.0",
    "rimraf": "^3.0.2",
    "tsup": "^5.11.13",
    "typescript": "^4.9.4"
  }
}
